# Прогресс

Четыре раздела. Держатся в актуальном состоянии — из них берётся контекст, когда
надо вспомнить, на чём остановились, и когда генерируются новые задачи.

---

## 1. Карта тем

Отмечай тему сделанной только после того, как тесты зелёные **и** пройдена проверка
понимания (объяснил решение своими словами, назвал две отвергнутые альтернативы,
ответил на вопрос «что сломается, если…»). Зелёные тесты сами по себе означают только,
что ты угадывал, пока не скомпилировалось.

| # | Готово | Дата | Тема | Что появляется в анализаторе | «Чистый код» | «Грокаем алгоритмы» |
|---|--------|---|---|---|---|---|
| 0 | [x]    | | Единицы трансляции, заголовки, компилятор против линкера | скелет проекта, который собирается | 5 | — |
| 1 | [x]    | 2026-09-05 | Классы, инкапсуляция, const-корректность | `Champion`, `PlayerStats`, KDA | 2, 6, 10 | — |
| 2 | [x]    | 2026-09-06 | Конструкторы, RAII, правило 0/3/5, move | владение данными матчей | 10 | — |
| 3 | [x]    | 2026-09-19 | Наследование, полиморфизм, интерфейсы | источники данных за интерфейсом | 6, 11 | — |
| 4 | [x]    | 2026-09-19 | Контейнеры STL | индекс матчей по чемпионам | — | 2, 5, 7, 8 |
| 5 | [ ]    | | Алгоритмы STL, лямбды, сортировка | топ чемпионов, фильтры | — | 1, 2, 4, 10 |
| 6 | [ ]    | | Строки и файловый ввод-вывод | чтение каталога с данными | 8 | — |
| 7 | [ ]    | | Ошибки и исключения | битые и неполные матчи | 7 | — |
| 8 | [ ]    | | Разбор JSON | реальные ответы Riot в мои типы | 8 | — |
| 9 | [ ]    | | Модульные тесты, чистые функции | тесты пишу я сам | 3, 9 | — |
| 10 | [ ]    | | Шаблоны и обобщённое программирование | обобщённые агрегаторы метрик | — | — |
| 11 | [ ]    | | Границы модулей, архитектура | разделение core / I/O / отчёты | 8, 11, 12 | — |
| 12 | [ ]    | | **Метрики и статистика** | слой `analysis/`: агрегация, медианы, интервалы | 3 | 1, 5 |
| 13 | [ ]    | | Сложность и оптимизация | быстрая агрегация | 17 | 1, 11, 12 |
| 14 | [ ]    | | Внешние зависимости | подключены nlohmann/json и libcurl | 8 | — |
| 15 | [ ]    | | HTTP-клиент, реальные запросы к Riot API | приложение само качает матчи | 7, 8 | — |
| 16 | [ ]    | | Кеширование, лимиты, устойчивость | офлайн-режим, повтор с backoff | 7, 17 | 5 |
| 17 | [ ]    | | Потоки и асинхронность | загрузка не морозит интерфейс | 13 | — |
| 18 | [ ]    | | **Основное приложение: Qt Quick (QML)** | окно с таблицами, фильтрами, графиками | 6, 11 | — |
| 19 | [ ]    | | **Оверлей: Dear ImGui + Live Client Data** | прозрачное окно поверх игры | 6, 11 | — |
| 20 | [ ]    | | Финальная сборка и релиз | упаковка, конфиг, документация | 14, 17 | 13 |

Решения по архитектуре — `project/ARCHITECTURE.md` (тема 11 их реализует, не обсуждает).
Ограничения Riot — `project/POLICY.md` (обязательны для всех задач темы 15 и дальше).

#### Тема 12: метрики и статистика

Появилась потому, что в карте не было темы про саму математику, а тема 13 (сложность) —
про другое. Строит слой `analysis/`.

- группировка и агрегация: `std::map`, `std::accumulate`;
- среднее, медиана, персентили;
- гистограммы (смерти по минутам — нужны таймлайны, отсюда `--timeline` уже сейчас);
- скользящие окна по последним N играм;
- интервал Уилсона для доли: он отвечает на вопрос «54% по 13 играм — это что-нибудь
  значит?», а нормальное приближение на малых выборках просто врёт;
- сравнение двух winrate;
- корреляция Спирмена.

Книги: «Грокаем алгоритмы» 1 и 5 — про структуры данных под агрегацию;
«Чистый код» 3 — держать всё это маленькими чистыми функциями.

Фича, которую коммерческие инструменты не делают: **отказываться печатать число, пока
выборка его не подтверждает**. Это встраивается в сами типы метрик — winrate носит свой
интервал с собой, и слой отчётов физически не может напечатать одно без другого.

#### Темы 18 и 19: почему интерфейс разделён

Два интерфейса решают разные задачи и сделаны на разных технологиях.

**Основное приложение — Qt Quick (QML).** Логика остаётся в C++, QML отвечает за
раскладку, стили и анимацию. Выбран вместо Dear ImGui, который создан для отладочных
панелей в immediate-mode и сопротивляется, когда его просят выглядеть как продукт.
Выбран вместо Slint потому, что когда упрёшься, у Qt есть ответ на любой вопрос,
а у Slint часто нет — а это решает всё, когда учишься один. Учти цену: MOC, свои
строки и контейнеры, signals-and-slots — это отдельная экосистема, и время на неё
надо запланировать.

**Оверлей — Dear ImGui.** Прозрачное окно поверх игры: always-on-top, click-through,
данные из Live Client Data API. GLFW даёт `GLFW_TRANSPARENT_FRAMEBUFFER` и
`GLFW_MOUSE_PASSTHROUGH`. Работает **только в borderless**: в exclusive fullscreen
оверлей не покажется никогда — это ограничение ОС, а не баг для отладки. Опрос раз
в секунду; сертификат на порту 2999 самоподписанный, поэтому `riotgames.pem` надо
добавить в доверенные.

Оверлей — это экзамен для правила границ. Если для его подключения надо поменять хотя
бы один заголовок в `core/`, архитектура была неверной: сначала говорим об этом
и чиним границу, потом пишем код оверлея.

**Веб-фронтенд (Electron или встроенный webview) рассмотрен и отложен.** Путь рабочий,
и половина индустрии живёт так, но это второй полноценный трек обучения поверх
Практикума и этого курса, и он сжал бы C++ в этом проекте до молотилки данных.
Если вернёшься к этому на теме 19 — слои оставляют выбор открытым, в этом их смысл.

> **Номера глав не сверены с бумажными книгами.** Взяты из оригинальных изданий.
> В русских переводах нумерация иногда съезжает. При первом обращении к главе сверь
> её со своим экземпляром и скажи, если разошлось — я поправлю таблицу и все `THEORY.md`.

Темы 0 и 1 сгенерированы полностью. Остальные генерируются по мере подхода — не раньше,
чем предыдущая закрыта, чтобы задачи учитывали накопившиеся слабые места.

### Задачи темы 0

Теория: `topics/00_build_model/THEORY.md`.
Эталонный разбор: `topics/00_build_model/demo/` — собирается и проходит `ctest`, чинить нечего.

| Готово | Эксперимент | Что должен понять |
|---|---|---|
| [x] | `e01_undefined_reference` | объявил, не определил → ошибка линкера |
| [x] | `e02_no_include_guard` | заголовок дважды → ошибка компилятора |
| [x] | `e03_odr_violation` | тело в заголовке, два `.cpp` → ошибка линкера |
| [x] | `e04_signature_mismatch` | сигнатуры разошлись → **та же** ошибка, что в e01 |

Собирать руками, команды в `topics/00_build_model/experiments/README.md`.

### Задачи темы 1

Теория: `topics/01_classes/THEORY.md`.

| Готово | Задача | Одна идея | Тест |
|---|---|---|---|
| [x] | `t01_champion` | класс = приватные данные + публичное поведение, инвариант держит конструктор | `topic01_t01_champion` |
| [x] | `t02_player_stats` | изменяемое состояние и единственный метод, который его меняет | `topic01_t02_player_stats` |
| [x] | `t03_champion_report` | const-методы вычисляют, а не хранят; возврат к `std::vector` из спринта 1 | `topic01_t03_champion_report` |

Один коммит на одну закрытую задачу, сообщение по-русски, пишешь его сам.

### Задачи темы 2

Теория: `topics/02_raii/THEORY.md`.

| Готово | Задача | Одна идея | Тест |
|---|---|---|---|
| [x] | `t01_match_file` | владение ресурсом уникально: конструктор захватывает, деструктор освобождает, копирование запрещено | `topic02_t01_match_file` |
| [x] | `t02_match_payload` | правило 3/5: класс с сырым буфером сам отвечает за копирование и перемещение | `topic02_t02_match_payload` |
| [x] | `t03_champion_index` | правило 0: поля управляют собой сами — писать нечего. Callback: внутри `ChampionReport` из темы 1 | `topic02_t03_champion_index` |

Задача 2.1 работает с настоящими фикстурами из `project/data/fixtures/` — путь
подставляет CMake. Задача 2.3 собирается вместе с `champion_report.cpp` из темы 1:
если там что-то сломается, отвалится и она.

### Задачи темы 3

Теория: `topics/03_polymorphism/THEORY.md`.

| Готово | Задача | Одна идея | Тест |
|---|---|---|---|
| [x] | `t01_match_source` | интерфейс: чисто виртуальные методы, виртуальный деструктор, `override` у наследника | `topic03_t01_match_source` |
| [x] | `t02_fallback_source` | реализация интерфейса, состоящая из реализаций того же интерфейса; переопределения объявляешь сам | `topic03_t02_fallback_source` |
| [x] | `t03_index_builder` | код против интерфейса: ни одного `if` про тип источника. Callback: `ChampionReport` (тема 1) и `ChampionIndex` (тема 2) | `topic03_t03_index_builder` |

Задача 3.2 на заглушках **не компилируется**, и это правильно: пока не объявишь три
переопределения, `FallbackMatchSource` остаётся абстрактным — `C2259`. Задача 3.3
собирается вместе с кодом тем 1 и 2: сломается там — отвалится и она.

### Задачи темы 4

Теория: `topics/04_containers/THEORY.md` — там же блок «ВЫПИСАТЬ В БЛОКНОТ»
со сжатой выжимкой: выбор контейнера, стоимость операций, `find` против `operator[]`,
`contains()`, `std::erase_if`, `std::flat_map` и печать контейнера через `std::println`.

| Готово | Задача | Одна идея | Тест |
|---|---|---|---|
| [x] | `t01_games_by_champion` | `std::map` вместо ручного поиска; чтение и запись — разные операции | `topic04_t01_games_by_champion` |
| [x] | `t02_champion_pool` | `std::set` для уникальности, словарь множеств для «уникальные у каждого». Callback: нормализация роли через `Champion` (тема 1) | `topic04_t02_champion_pool` |
| [x] | `t03_index_by_map` | `try_emplace` вместо «найти или создать» руками; смена контейнера меняет контракт (порядок). Callback: сравнение с 3.3 | `topic04_t03_index_by_map` |

Задача 4.3 собирается вместе с задачей 3.3 и сравнивает с ней результат: сломается
там — отвалится и она. Задача 4.2 тянет `champion.cpp` из темы 1.

### Следующие действия

1. **Если ещё не выгружены — выгрузить матчи с `--timeline`.**
   Ключ живёт 24 часа, а данным надо накопиться до того, как они понадобятся теме 6.
   История матчей конечна: то, что выпало, добрать нельзя.
   `python scripts/fetch_matches.py --riot-id "Имя#TAG" --region europe --queue 420 --count 500 --timeline`
2. Тема 4 закрыта целиком (см. журнал ниже). Тема 5 (алгоритмы STL, лямбды,
   сортировка) ещё не сгенерирована — начинать с разговора о том, что уже
   знает из Практикума, потом THEORY.md и три задачи.
3. Мелочь в `topics/01_classes/tasks/t03_champion_report/champion_report.cpp:31`:
   `return lines_.size();` даёт `warning C4267` (`size_t` → `int`). Тесты зелёные,
   но предупреждение будет всплывать в каждой сборке, которая тянет этот файл.
   Чинится явным `static_cast<int>`.
4. Не забыть `#include <utility>` в `topics/03_polymorphism/tasks/t03_index_builder/index_builder.cpp` —
   `std::move` там сейчас собирается только транзитивным инклюдом.

### Playground

`playground/` — одна программа из всех реализованных задач: создаёт твои классы
и печатает, что они возвращают на подготовленных данных. Не тест, в `ctest`
не входит, на закрытие тем не влияет.

```
cmake --build build --config Debug --target playground
build\playground\Debug\playground.exe
```

Данные пока собраны руками в `SampleEntries()` внутри `playground/main.cpp`:
чтение JSON появится в теме 8, обход каталога — в теме 6. Файл мой, правь свободно;
по мере прохождения тем в него добавляются новые секции.

---

## 2. Слабые места

Что осталось непонятым. Отсюда берутся дополнительные задачи и сюда же попадает всё,
что вызвало «а почему так?» без внятного ответа. Пустой раздел на старте — это нормально,
пустой раздел через месяц означает, что его перестали заполнять.

**Тема 0. Три причины одной ошибки LNK2019.** По логам от 2026-09-01 ты словил её трижды,
и каждый раз по разной причине:

| Где | Почему линкер не нашёл символ |
|---|---|
| e01 | определения не существует вообще |
| e03 | определение есть в `kda.cpp`, но файл не передали компилятору (`cl main.cpp report.cpp` без `kda.cpp`) |
| e04 | определение есть, но с другой сигнатурой — MSVC даже подсказал: `Указание на символы, которые определены и могут подойти` |

Третья строка в исходной карточке темы не описана — это находка из твоего лога, и она
самая частая в реальной работе: файл забыли добавить в сборку. В CMake это выглядит как
«забыл дописать `.cpp` в `course_add_task`».

**Тема 0, закрыто.** LNK2005 воспроизведён и увиден: `report.obj : error LNK2005
... ComputeKda ... уже определён в main.obj`. Причина подтверждена — тело функции
в заголовке, включённом в две единицы трансляции. Почини правильно: `inline` перед
`double ComputeKda(...)` в `kda.h`.

**Тема 0, вывод по e04 неточен.** Записано «так можно делать, но нужно создавать ещё
перегрузки». Перегрузки здесь ни при чём — они бывают нужны, но эту ошибку не лечат.
Верный вывод: объявление и определение должны совпадать посимвольно, иначе получаются
две разные функции, и та, что объявлена, остаётся без тела.

**Тема 1, `t01_champion`.** `CheckRole`/`CheckName` не обрезают пробелы по краям
(`"  middle "` останется `"  MIDDLE "`, инвариант формально не нарушен, но
`DisplayName()` выглядит криво). Спецификация задачи этого не требовала, тесты не
ловят — не баг, но всплывёт в теме 6 при парсинге реальных строк, тогда добавить `trim`.

**Тема 1, `t02`/`t03`. Порядок `static_cast` относительно деления.** В `PlayerStats`
всё было верно с первого раза, но в `ChampionReport::Kda()` и `CsPerMinute()` каст
к `double` несколько раз оказывался *после* целочисленного деления (один раз —
внутри цикла, на каждой итерации, что ещё и роняет точность накопительно, не
только один раз в конце). Чинил сам после одного указания на паттерн «каст до
операции, не после», но паттерн повторился дважды подряд — стоит явно
проверять на следующих задачах с метриками (тема 12 особенно).

**Тема 1, `t03_champion_report`. Границы диапазона.** `HasEnoughData()` сначала
использовал строгое `>` там, где формулировка «хотя бы N» требует `>=` —
классический off-by-one на границе.

**Тема 1, `t03_champion_report`. Деление на ноль от невнимательного чтения ветки.**
В первой версии `Kda()` ветка «`deaths == 0`» делила именно на `deaths` (то есть
на 0) вместо деления на 1 — при чтении своего же условия невнимательно понял,
какая переменная должна стоять в знаменателе после if.

**Тема 1, повторяющийся паттерн. Мёртвые `// TODO` после реализации.** Встретилось
в `t01`, и первые правки `t02`/`t03` тоже оставляли комментарий-заглушку рядом
с уже написанным телом. К закрытию `t03` это уже не повторялось — похоже, паттерн
усвоен, но стоит перепроверить в начале темы 2.

**Тема 2. Указатели/владение сырым ресурсом — абстракция класса ломает понимание.**
На `t01_match_file` разобрались (указатель, `nullptr`, RAII на уровне идеи), но на
`t02_match_payload` застрял именно на связке «указатель как поле класса + new[]/delete[]
+ конструктор/деструктор одновременно» — слишком много новых понятий разом, без
возможности запустить и увидеть результат (класс без `main`, абстрактный тест).
Первая попытка помочь была неудачной: показал демо в `practice_code/pr.cpp` совсем
без класса (голый `main`, `fopen`/`fseek`/`ftell`/`fread`) — это сняло тревогу от
чтения файла, но не дало ничего для реальной проблемы (класс + владение указателем).
Вывод на будущее: когда затык именно в «класс + сырой ресурс», нужен **маленький
ручной класс** (не текущая задача с курса, отдельный сэндбокс), где видно
конструктор/деструктор в работе — печать «выделяю»/«освобождаю» при создании и
уничтожении объектов в цикле/scope, — а не голая функция без единого класса.
Также: не смешивать новую тему (файловый ввод-вывод, `fseek`/`ftell`/`fread`) с
темой, которую разбираем (владение ресурсом в классе) — лишние незнакомые функции
только шумят.

**Тема 3, `t03_index_builder`. Не считывается постановка задачи целиком.** Главный
затык был не в полиморфизме: непонятно было, *что вообще за функция* требуется и
какие роли у `MatchEntry` (сырая запись, одноразовая), `ChampionReport` (накопитель
на одного чемпиона, их много) и `ChampionIndex` (финальная коробка, одна). Сказано
прямо: критерии в шапке `.h` не читаются, работа идёт по `// TODO` рядом с телом.
Все требования и сигнатуры перенесены в TODO внутрь `.cpp` — так и генерировать
дальше. Плюс в тему 4 нужна первая задача, где явно расписан поток данных
«что на входе → что накапливается → что на выходе», а не только сигнатура.

**Тема 3, повторяющийся паттерн. `std::move` не ставится при передаче в контейнер.**
Третий раз подряд (темы 1, 2, теперь 3.3: `push_back(rpt)` и `AddReport(report)`).
Правило простое и его стоит проговаривать вслух на каждой такой строке: именованная
переменная — всегда lvalue, даже в последней строке своей жизни; компилятор сам
перемещение не подставит. Проверять в первой же задаче темы 4 (контейнеры — там
таких мест будет много).

**Тема 4, `t03_index_by_map`. «Убрать цикл» понято как «убрать все циклы».**
Когда вложенный цикл из 3.3 заменился на `try_emplace`, естественно решил, что
раз цикла «нет» — то и второго цикла (перенос отчётов в индекс) быть не должно.
Путал вложенность (`O(a×b)`, стоимости перемножаются) с последовательностью
(`O(a+b)`, стоимости складываются) — само понятие сложности пока нетвёрдое,
не только конкретный пример. Держать в уме для темы 5 (там впервые формально
считаем стоимость алгоритмов на реальных данных) и явно вернуться в теме 13.

---

## 3. Журнал сессий

Одна строка на сессию: дата, что делал. Ради этого раздела всё и затевалось —
он позволяет вернуться после перерыва, не начиная с нуля.

| Дата | Что было |
|---|---|
| 2026-09-01 | Развёрнут курс: сборка, doctest, темы 0 и 1, данные и фикстуры. Проверено: тема 0 собирается и проходит тесты, тесты темы 1 падают из-за пустых заглушек |
| 2026-09-01 | Тема 0: прогнаны все четыре эксперимента, все четыре чинятся и собираются. В e03 заведён `report.h`. Открыто: LNK2005 вживую не видел |
| 2026-09-01 | Тема 0 закрыта: e03 доисправлен через `inline`, LNK2005 воспроизведён и понят |
| 2026-09-02 | Тема 1, `t01_champion` закрыта: конструктор через список инициализации, нормализация имени/роли вынесена в анонимный namespace, `HasRole` сравнивает через `CheckRole`. Тесты зелёные, понимание подтверждено. Дальше: `t02_player_stats` |
| 2026-09-05 | Тема 1 закрыта целиком. `t02_player_stats`: список инициализации с `std::move` сразу верный, `AddMatch` — валидация до мутации, все геттеры и `Losses()` через `Games() - Wins()`. `t03_champion_report`: `Add()` с приватной `IsValid` (перенесена в анонимный namespace после указания на внешнее связывание), `Kda()`/`Winrate()`/`CsPerMinute()`/`HasEnoughData()` — по ходу поймано и исправлено: деление на 0 в `Kda()`, порядок `static_cast` (дважды), инвертированный перевод секунд в минуты, лишнее `if (line.minions)`, off-by-one в `HasEnoughData()`. Тесты зелёные. Понимание подтверждено: объяснил своими словами разницу суммы по выборке vs среднего по матчам для KDA, назвал проблему производительности при пересчёте метрик на каждый кадр UI над большим `lines_` и предложил кеширование как один из вариантов (вернуться к этому компромиссу в теме 12). Дальше: тема 2 — конструкторы, RAII, правило 0/3/5, move |
| 2026-09-06 | Тема 2 закрыта: `t01_match_file` (владение файлом, копирование запрещено), `t02_match_payload` (правило 3/5 на сыром буфере), `t03_champion_index` (правило 0, callback на `ChampionReport`). Все тесты зелёные, коммиты `4183e86`, `99ab5ba`. Сгенерирована тема 3: THEORY + три задачи. Дальше: `t01_match_source` |
| 2026-09-18 | Возврат после перерыва в 12 дней. Прогнан весь набор тем 0–2: 7/7 зелёные, регрессий нет. Тема 3 не начата — заглушки нетронуты. План: повторно с нуля решить `t02_match_payload` (правило 3/5 выветривается первым), потом `t01_match_source` |
| 2026-09-19 | Тема 3 закрыта целиком. `t01_match_source`: сначала пропущено требование «недоступный источник → пустой вектор» (тест поймал), исправлено. `t02_fallback_source`: `Active()` сразу верный, но во всех трёх переопределениях не было проверки на `nullptr` — исправлено после наводящего вопроса. `t03_index_builder`: потребовалось три уровня подсказок, главная трудность — не сама тема, а непонимание ролей `MatchEntry`/`ChampionReport`/`ChampionIndex` и того, что это вообще за функция. `std::move` в обоих местах забыт, добавлен после напоминания. Все 10 тестов курса зелёные. Понимание подтверждено: полиморфизм вместо `if` по типу, вложенность `FallbackMatchSource` в самого себя, стоимость линейного поиска на 100k записей. Собран `playground/` — одна программа из всех задач тем 1–3 на подготовленных данных |
| 2026-09-19 | Курс переведён с C++17 на C++23: `CMAKE_CXX_STANDARD 23` + `cxx_std_23`, `cmake_minimum_required` 3.20. MSVC 14.51 собирает всё как `/std:c++latest` (флаг `/std:c++23` cl молча игнорирует — это записано в README и в `experiments/README.md`). Проверено на этой машине: `std::print`/`std::println`, форматирование контейнеров, `contains()`, `std::erase_if`, прозрачный `std::less<>`, `<flat_map>`, `std::expected`, `ranges::to`, `views::zip`/`enumerate`, deducing this, `import std;`. Пересобран весь курс: тесты тем 0–3 зелёные (10/10), падают только три пустые заглушки темы 4. Переписаны все пять `THEORY.md`: тема 0 — почему модули есть, но курс остаётся на `#include`; тема 1 — `[[nodiscard]]`, `string_view`, `std::format`; тема 2 — `std::exchange`, `unique_ptr`, `std::expected` как форвард на тему 7; тема 3 — `final`, владение через `unique_ptr`, альтернативы динамическому полиморфизму; тема 4 — `contains()`, `erase_if`, `flat_map`, прозрачный компаратор, `std::println` |
| 2026-09-19 | Тема 4 закрыта целиком. `t01_games_by_champion`: `operator[]` на месте нужен для записи (`++games[name]`), для чтения выбрал `contains()`+`at()` — после вопроса про двойной поиск переписал на одиночный `find()`; `MostPlayedChampion` — сначала `std::max` без обновления имени и без `return` в конце, исправлено на ручной `if` с бегущим максимумом. `t02_champion_pool`: дважды один и тот же баг — `champion.DisplayName()` вместо `champion.Name()` как ключ (роль просачивалась в ключ, две роли одного чемпиона считались разными записями); отсутствовала проверка на пустое имя. Оба найдены до сборки, по review. `t03_index_by_map`: не сразу понял, что убирается вложенный цикл, а не циклы вообще — прояснили на `O(a×b)` против `O(a+b)`; сам `try_emplace` дался легко, дальше — шаг за шагом. Все 13 тестов курса зелёные. Понимание подтверждено: почему `set` для `champions_`, и что `operator[]` на месте `find()` в `RolesOf` либо не скомпилируется (const), либо молча заведёт мусорные записи в словарь (не-const). Слабое место: путал «нет вложенного цикла» с «нет циклов вообще» — расходы по стоимости алгоритмов пока держатся нетвёрдо, взять на заметку для темы 13 |

---

## 4. Темп

**Пять-семь часов в неделю. Двадцать одна тема — это восемь-двенадцать месяцев.**

Раньше здесь стояло «девятнадцать тем, шесть-девять месяцев». После добавления темы 12
(метрики) и разделения интерфейса на Qt Quick и оверлей курс стал длиннее — это сказано
прямо, а не оставлено тихо разъезжаться с планом.

Это сказано сразу и честно, потому что курс, который притворяется короче, чем он есть,
бросают на втором месяце. Здесь нет дедлайнов, и это его главная слабость: единственное,
что доводит такие курсы до конца, — регулярность, а не интенсивность.

Три правила, которые важнее скорости:

- **Практикум выигрывает всегда.** У него дедлайны, у этого курса их нет. Спринт-запара —
  ставим на паузу, без чувства вины.
- **Сорок минут в тупике — берём подсказку.** Сидеть до двух ночи над одной функцией
  не героизм: так убивается та самая регулярность.
- **Одна задача — одно сидение, 30–90 минут.** Если задача не влезает, значит она
  неправильно нарезана, и это моя ошибка, а не твоя. Скажи — переразобью.
