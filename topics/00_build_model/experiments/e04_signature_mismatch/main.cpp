// Эксперимент 4: объявление и определение разошлись в типе параметра.
//
// Собрать:
//   cl /nologo /std:c++latest /EHsc main.cpp kda.cpp
//   g++ -std=c++23 main.cpp kda.cpp -o main
//
// Ожидаемая ошибка: та же LNK2019 / undefined reference, что и в e01.
//
// В этом весь урок. Ошибка одна, причины разные:
//   e01 — функцию забыли написать;
//   e04 — функцию написали, но с другой сигнатурой.
//
// Отличить можно только по имени символа в тексте ошибки. MSVC печатает
// декорированное имя вида ?ComputeKda@@YANHHH@Z, GCC — читаемое
// ComputeKda(int, int, int). Сравни его с тем, что реально лежит в kda.cpp,
// и расхождение станет видно.
//
// Почини: см. комментарий в kda.h.

#include <iostream>

#include "kda.h"

int main() {
    std::cout << ComputeKda(10, 5, 5) << '\n';
    return 0;
}
